# ML dependencies for Jetson deployment with Python 3.11
# These versions are tested and compatible with:
#   - Python 3.11.0rc1
#   - JetPack R36.5 (aarch64)
#   - F-Prime flight software

# Core ML framework
torch==2.4.0
transformers==4.33.0
datasets==2.13.0

# Image processing and utilities
pillow>=10.0.0
numpy<2.0

# Additional dependencies (included transitively but pinned for stability)
huggingface-hub>=0.16.0
tokenizers>=0.13.0
safetensors>=0.3.1
tqdm>=4.62.0
